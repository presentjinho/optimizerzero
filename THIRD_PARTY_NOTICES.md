# Third-party notices

OptimizerZero 자체 코드는 `LICENSE`의 MIT 조건으로 배포됩니다.

웹 배포본은 아래 라이브러리 또는 그 일부를 로컬에 포함합니다. 원문 라이선스는 `web/vendor/`에 함께 보관됩니다.

- JSZip — MIT 또는 GPL-3.0 중 MIT 선택 (`web/vendor/JSZIP_LICENSE.markdown`)
- pdf-lib — MIT (`web/vendor/PDF_LIB_LICENSE.md`)
- PDF.js — Apache-2.0 (`web/vendor/pdfjs/PDFJS_LICENSE`)
- jSquash AVIF/JPEG XL encoder 구성요소 — Apache-2.0 (`web/vendor/JSQUASH_LICENSE.md`)

데스크톱 선택 기능은 Pillow, PyMuPDF, pikepdf, pillow-heif를 사용할 수 있습니다. 설치되는 정확한 버전과 전이 의존성은 사용자의 Python 환경에서 결정되며 각 패키지의 라이선스가 적용됩니다.
